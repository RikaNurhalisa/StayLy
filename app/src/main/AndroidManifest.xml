<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.stayly"> <!-- Ganti dengan nama package kamu jika perlu -->

    <!-- Tambahkan izin INTERNET dengan benar -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_laucher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_laucher"
        android:supportsRtl="true"
        android:theme="@style/Theme.stayly"
        tools:targetApi="31">

        <activity android:name=".AccountActivity"/>
        <activity android:name=".FavoritActivity" />
        <activity android:name=".ChatActivity" />


        <activity
            android:name="com.example.stayly.HomeActivity"
            android:exported="false" />
        <activity
            android:name="com.example.stayly.DaftarActivity"
            android:exported="false" />
        <activity
            android:name="com.example.stayly.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
